root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/worker ./cmd/worker"
  bin = "tmp/worker"
  full_bin = "./tmp/worker"
  include_ext = ["go"]
  exclude_dir = ["tmp", "storage"]

[log]
  time = true
